<turbo-frame id="mentions">
  <% @users.each do |user| %>
    <button type="button" name="sgid" value="<%= user.attachable_sgid %>" id="<%= dom_id user, :mention %>" role="option" data-action="click->mentions#insert">
      <%= render partial: user.to_trix_content_attachment_partial_path, object: user, as: :user %>
    </button>
  <% end %>
</turbo-frame>
